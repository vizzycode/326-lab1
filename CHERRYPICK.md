# Cherry-Pick

`bba20f2` on `lab09-feature` and `a4cc319` on `lab09-hotfix` contain the exact same change, but are two different commits with two different hashes. `git cherry-pick` copies a commit, it does not move it.
